
'''
    resolve_question: takes in raw text as input and returns the canonical question key if present
'''
def resolve_canonical_question(raw_question: str):
    # if in database, return the canonical question else return None
    return ""

